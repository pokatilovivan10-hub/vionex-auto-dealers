# Установка VIONEX LEADS v3.10.0

1. Загрузить `vionex-leads-update-v3.10.0.zip` в `/var/www/bitrix2/data/www/vionex.ru`.
2. Проверить размер и `unzip -t`.
3. Распаковать в `/root`.
4. Выполнить `bash APPLY_UPDATE.sh`.
5. Проверить `/health` и `systemctl status vionex-leads`.
