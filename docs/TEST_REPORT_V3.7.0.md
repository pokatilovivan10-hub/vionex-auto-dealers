# Отчёт тестирования VIONEX LEADS v3.7.0

## Результат

- Node.js-тесты: 15 / 15 пройдены.
- Ошибки тестов: 0.
- Ошибки браузерной консоли: 0.
- Горизонтальное переполнение: 0 на всех проверенных ширинах.

## Проверенные разрешения

- 1440 × 1000, тёмная тема;
- 1440 × 1000, светлая тема;
- 1024 × 900;
- 768 × 1024;
- 390 × 844;
- 360 × 800, светлая тема.

## Проверка структуры страницы

На каждом разрешении подтверждено:

- H1: 1;
- метрики: 4;
- тизеры: 3;
- ключевой кейс: 1;
- видеоплеер-фасад: 1;
- автокейсы: 5;
- точки карусели: 5;
- карточки возможностей: 6;
- карточки человеческого контроля: 3;
- тарифные планы: 3;
- AI-агенты: 5;
- FAQ: 6;
- FAQPage schema: присутствует;
- Organization schema: присутствует;
- CTA, открывающие форму: 12;
- дублирующиеся HTML id: 0;
- повреждённые изображения: 0.

## Серверные проверки

Подтверждены:

- `/health` возвращает версию `3.7.0` и `cms: true`;
- `/services/auto-dealers` возвращает 200;
- динамические страницы сервисов, кейсов и блога работают;
- `/sitemap.xml` содержит новую страницу;
- canonical redirects работают;
- валидная заявка сохраняется;
- невалидная заявка отклоняется;
- first-party события принимаются без сохранения IP;
- вход в CMS, черновик, предпросмотр, публикация и медиатека работают;
- миграция схемы до версии 8 не перезаписывает пользовательские тексты.

## Безопасность

- новая страница использует существующую CSRF-защиту и серверную валидацию форм;
- секреты не добавлены в исходный код;
- произвольный HTML/JS из CMS не выполняется;
- видеоплеер разрешает только безопасно сформированные YouTube/Vimeo embed-адреса;
- CSP ограничивает `frame-src` доменами YouTube No-Cookie и Vimeo;
- боевые CRM-изменения обновление не выполняет.

## Ограничения проверки

- Внешний PageSpeed Insights не запускался на локальном URL.
- Фактическая передача в конкретную CRM зависит от текущего `.env` и webhook заказчика.
- Реальный видеокейс не предоставлен; проверен ленивый фасад и разрешённая ТЗ заглушка.

## Проверка архивов и обновления

- полный ZIP проходит `unzip -t`;
- ZIP обновления проходит `unzip -t`;
- чистая распаковка полного ZIP проходит `npm run check` и запускает страницу `/services/auto-dealers`;
- обновление протестировано на изолированной копии v3.6.1;
- после обновления сохранены `.env`, `data/uploads`, заявки и существующая CMS-база;
- миграция добавила нишевую страницу и подняла `schema_version` до 8;
- ручной откат вернул код и CMS-базу v3.6.1 без потери пользовательских файлов.
