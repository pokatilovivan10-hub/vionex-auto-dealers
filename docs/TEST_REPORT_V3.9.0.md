# TEST REPORT — VIONEX LEADS v3.9.0

## Автоматические проверки

- `npm test`: 15 тестов пройдено, ошибок 0.
- `npm run check`: проверяет структуру проекта, версии, CMS, миграции, безопасность, разметку и синтаксис.

## Проверено

- версия `/health` — `3.9.0`;
- страница `/services/auto-dealers` рендерится из CMS;
- на странице 8 активных блоков;
- удалены повторяющиеся `cases-slider`, `capabilities`, `human-control`;
- работают Hero, метрики, контур заявки, кейсы, CRM, AI-агенты, тарифы, FAQ и CTA;
- миграция CMS обновляет действующую страницу до нового порядка блоков;
- пользовательская база, `.env`, загрузки и заявки не входят в payload обновления;
- синтаксис Node.js-файлов проверяется автоматически;
- форма и защищённая CMS сохраняют существующую серверную архитектуру.

## Ручная проверка после установки

1. Открыть `/services/auto-dealers` в тёмной и светлой теме.
2. Проверить desktop и мобильный экран.
3. Открыть FAQ.
4. Открыть форму с кнопки «Получить план».
5. Отправить одну тестовую заявку и проверить `data/leads.ndjson` и настроенный webhook/CRM.

## Проверка пакета обновления

Обновление установлено на изолированную копию v3.8.1 с тестовой службой:

- `APPLY_UPDATE.sh` завершился сообщением `Update completed successfully`;
- `/health` вернул `version=3.9.0`, `cms=true`;
- затем выполнен `ROLLBACK.sh`;
- после отката `/health` снова вернул `version=3.8.1`;
- `.env` и каталог `data/` сохранялись вне payload.
